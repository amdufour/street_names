{
  "scripts": {
    "start": "parcel src/index.html",
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url 'https://amdufour.github.io/street_names/'",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@parcel/config-default": "^2.8.3",
    "d3": "^7.8.2",
    "gh-pages": "^5.0.0",
    "parcel-transformer-csv": "^0.0.3"
  }
}
